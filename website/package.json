{
  "name": "task-pipeline-website",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site for the task-pipeline Claude Code plugin",
  "type": "module",
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
