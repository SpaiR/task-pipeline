---
type: regex
target: last_message
match: contains
pattern: 'Architecture:(\*\*)?\s*\d+ components?'
---
The digest reports the section's shape — components, interfaces, item sketches —
then one line per component, the user's one glance to catch a misplaced
boundary. Matched on content, not position: a run may render the template's
`Architecture:` label in bold or inside a list item.
